# 事業所サイト（admin-front）

## git clone

```bash
git clone https://gitlab.coresoft-net.com/sensy/ai-automation-order-system/admin-front.git
```

## nuxt の起動

1. クローンしたフォルダを `vscode` で開く
1. （初回のみ）`yarn install` を実行
1. `yarn dev` を実行

ターミナルに url が表示されると思うので、そこを ctrl + click して ブラウザ上で表示できれば完了。
